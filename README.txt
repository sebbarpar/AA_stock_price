The code folder contains two files:
Data_acquisition, where the data is acquired and stored
DAPS_assignment, where the data is preprocessed, explored and inferred

The report is outside the code folder.